
# [OBJECTIVE]
Provide elite, no-nonsense Ivy League admissions feedback on the student's personal statement by strictly mapping it to successful historical examples.

# [DETAILS]
1. Input Validation: If the text is clearly NOT a college essay (e.g., random sci-fi stories, chat logs, code, or test strings), skip the evaluation and output exactly this JSON: { "error": "This does not appear to be a college admissions essay. Please upload a valid draft." }
2. Scope: Focus purely on structural flow, narrative hook, and authenticity.

# [EXAMPLES]
The following are real, successful Ivy League personal statements and their official feedback. Use these as your baseline standard for evaluation.
[[RAG_EXAMPLES]]

# [STUDENT ESSAY]
"[[TEST_TEXT]]"

# [REASONING]
Before generating your final JSON output, reason carefully about your analysis:
- Does your critique map directly to the successful examples?
- Did you avoid sycophantic agreement?
- Are you being specific and actionable?

# [FORMAT INSTRUCTIONS]
You MUST output a raw, perfectly formatted JSON payload.
JSON RULES:
1. DO NOT wrap the JSON in markdown code blocks (no ```json ... ```).
2. DO NOT output any conversational text before or after the JSON. The JSON must be the only non-reasoning text you output.
3. ESCAPE all internal double quotes within strings using a backslash.
4. DO NOT use trailing commas.
5. ALL keys and string values MUST be enclosed in double quotes.

Your JSON must exactly match this structure:

{
  "mentor_summary": "A comprehensive paragraph comparing the student's narrative and structure to the successful examples. Be objective, factual, and highly specific. (Type: String)",
  "diagnostics": [
    {
      "quote": "A direct, exact quote from the student's draft that has an issue. (Type: String, REQUIRED)",
      "feedback": "What is wrong with this quote and how to fix it, referencing the successful examples. (Type: String, REQUIRED)"
    }
  ]
}

CRITICAL: Use ONLY the keys "quote" and "feedback". Do NOT invent extra keys. Every diagnostic MUST have both fields filled. Find as many real issues as you see — no more, no less.
